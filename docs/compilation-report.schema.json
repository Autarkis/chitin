{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://github.com/Autarkis/chitin/blob/main/docs/compilation-report.schema.json",
  "title": "Chitin CompilationReport v1",
  "type": "object",
  "additionalProperties": false,
  "required": [
    "report_version",
    "status",
    "profile",
    "verdict",
    "input",
    "output",
    "timings_ms",
    "warnings",
    "metrics",
    "processing",
    "runtime",
    "reproducibility",
    "config",
    "artifacts",
    "build_identity"
  ],
  "properties": {
    "report_version": { "const": 1 },
    "status": { "enum": ["complete", "rejected"] },
    "profile": { "type": ["string", "null"] },
    "verdict": {
      "type": "object",
      "additionalProperties": false,
      "required": ["profile", "status", "reasons", "checks"],
      "properties": {
        "profile": { "type": ["string", "null"] },
        "status": { "enum": ["pass", "fail", "not_evaluated"] },
        "reasons": { "type": "array", "items": { "type": "string" } },
        "checks": {
          "type": "array",
          "items": {
            "type": "object",
            "additionalProperties": false,
            "required": ["code", "status", "message"],
            "properties": {
              "code": { "type": "string" },
              "status": { "enum": ["pass", "fail", "not_evaluated"] },
              "message": { "type": "string" },
              "suggestion": {
                "type": "string",
                "description": "Actionable hint for resolving a failed check"
              }
            }
          }
        }
      }
    },
    "input": {
      "type": "object",
      "additionalProperties": false,
      "required": ["kind", "source_vertices", "processed_vertices", "mesh_vertices"],
      "properties": {
        "kind": { "type": "string" },
        "source_vertices": { "type": "integer", "minimum": 0 },
        "processed_vertices": { "type": "integer", "minimum": 0 },
        "mesh_vertices": { "type": "integer", "minimum": 0 }
      }
    },
    "output": {
      "type": "object",
      "additionalProperties": false,
      "required": ["collider_kind", "hull_count", "vertex_count", "triangle_count", "lod_tier_count", "byte_length"],
      "properties": {
        "collider_kind": { "type": "string" },
        "hull_count": { "type": "integer", "minimum": 0 },
        "vertex_count": { "type": "integer", "minimum": 0 },
        "triangle_count": { "type": "integer", "minimum": 0 },
        "lod_tier_count": { "type": "integer", "minimum": 0 },
        "byte_length": { "type": ["integer", "null"], "minimum": 0 }
      }
    },
    "timings_ms": {
      "type": "object",
      "additionalProperties": { "type": "number", "minimum": 0 }
    },
    "warnings": {
      "type": "array",
      "items": { "$ref": "#/$defs/warning" }
    },
    "metrics": {
      "type": "object",
      "additionalProperties": { "$ref": "#/$defs/metric" }
    },
    "processing": {
      "type": "object",
      "additionalProperties": false,
      "required": ["pipeline", "fallbacks", "refinements"],
      "properties": {
        "pipeline": { "type": "array", "items": { "type": "string" } },
        "fallbacks": {
          "type": "object",
          "additionalProperties": false,
          "required": ["planar_substitute_hulls"],
          "properties": {
            "planar_substitute_hulls": { "type": ["integer", "null"], "minimum": 0 }
          }
        },
        "refinements": {
          "type": "object",
          "additionalProperties": false,
          "required": ["snug_fit"],
          "properties": {
            "snug_fit": {
              "type": "object",
              "additionalProperties": false,
              "required": ["status", "refined_hulls", "rejected_hulls", "skipped_hulls"],
              "properties": {
                "status": { "enum": ["applied", "skipped", "not_requested", "unknown"] },
                "refined_hulls": { "type": ["integer", "null"], "minimum": 0 },
                "rejected_hulls": { "type": ["integer", "null"], "minimum": 0 },
                "skipped_hulls": { "type": ["integer", "null"], "minimum": 0 }
              }
            }
          }
        }
      }
    },
    "runtime": {
      "type": "object",
      "additionalProperties": false,
      "required": ["kind", "implementation", "version", "compiler_version", "dependencies"],
      "properties": {
        "kind": { "type": "string" },
        "implementation": { "type": "string" },
        "version": { "type": "string" },
        "compiler_version": { "type": "string" },
        "dependencies": {
          "type": "object",
          "additionalProperties": { "type": "string" }
        }
      }
    },
    "reproducibility": {
      "type": "object",
      "additionalProperties": false,
      "required": ["scope", "deterministic", "artifact_sha256"],
      "properties": {
        "scope": { "const": "same_runtime_toolchain" },
        "deterministic": { "type": ["boolean", "null"] },
        "artifact_sha256": {
          "anyOf": [
            { "type": "string", "pattern": "^[0-9a-f]{64}$" },
            { "type": "null" }
          ]
        }
      }
    },
    "config": {
      "type": "object",
      "additionalProperties": false,
      "required": ["requested", "effective"],
      "properties": {
        "requested": { "type": ["object", "null"] },
        "effective": { "type": ["object", "null"] }
      }
    },
    "artifacts": {
      "type": "object",
      "additionalProperties": { "type": "string" }
    },
    "build_identity": {
      "type": "object",
      "description": "Three-layer provenance identity (lingot #64 / chitin #100). Logical build identity = what was asked; realization = how it was built; artifact_digest = what came out.",
      "additionalProperties": false,
      "required": [
        "effective_input_digest",
        "algorithm_version",
        "numerical_policy_version",
        "config_digest"
      ],
      "properties": {
        "effective_input_digest": {
          "type": ["string", "null"],
          "description": "SHA-256 of post-preprocessing canonical geometry entering decomposition. Not the source file hash. Null when not computable (e.g. browser WASM without preprocessing access).",
          "pattern": "^(sha256|blake3):[0-9a-f]+$"
        },
        "algorithm_version": {
          "type": "string",
          "description": "Semantic version of the decomposition algorithm, independent of chitin package version.",
          "pattern": "^[0-9]+\\.[0-9]+\\.[0-9]+$"
        },
        "numerical_policy_version": {
          "type": "string",
          "description": "Version of acceptance thresholds and profile definitions. Changes invalidate verdicts, not geometry.",
          "pattern": "^[0-9]+\\.[0-9]+\\.[0-9]+$"
        },
        "config_digest": {
          "type": ["string", "null"],
          "description": "SHA-256 of output-affecting resolved config (excludes timeout, deterministic, operational settings). Null when config hashing is not available.",
          "pattern": "^(sha256|blake3):[0-9a-f]+$"
        }
      }
    }
  },
  "$defs": {
    "warning": {
      "type": "object",
      "additionalProperties": false,
      "required": ["code", "severity", "message", "context"],
      "properties": {
        "code": { "type": "string" },
        "severity": { "enum": ["info", "warning", "error"] },
        "message": { "type": "string" },
        "context": { "type": "object" }
      }
    },
    "metric": {
      "type": "object",
      "additionalProperties": false,
      "required": ["value", "unit", "status"],
      "properties": {
        "value": { "type": ["string", "number", "boolean", "null"] },
        "unit": { "type": "string" },
        "status": { "enum": ["measured", "not_measured", "not_applicable"] }
      }
    }
  }
}
